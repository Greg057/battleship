import ComputerAI from "../script/computerAI";
import Player from "../script/player";

test("AI attacks correctly", () => {
    const computer = new ComputerAI()
    const player = new Player()
    /* computer.randomAttack(player.gameboard)
    player.gameboard.board */
})